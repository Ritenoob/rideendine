#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-commit hooks..."

# Run lint-staged
npx lint-staged

# Run tests on changed files (optional, can be slow)
# npm run test -- --bail --findRelatedTests

echo "Pre-commit checks passed ✅"
