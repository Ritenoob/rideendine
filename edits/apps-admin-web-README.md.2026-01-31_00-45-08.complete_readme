# Admin Web (Prototype)

Static React-in-the-browser admin panel prototype for Phase 4.

## Run
```bash
python3 -m http.server 3002 --directory apps/admin-web
```
Open `http://localhost:3002`.

## Notes
- Uses live API data from `http://localhost:9001` (configurable).
- Login uses `/auth/login` and stores the access token locally in the UI.
- All admin endpoints require an admin JWT.
